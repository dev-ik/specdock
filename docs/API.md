# API

Source of truth:

See [API_CONTRACTS.md](./API_CONTRACTS.md).

This file intentionally does not duplicate endpoint contracts to avoid drift.

Current API prefix:

```txt
/api
```

Endpoints:

```txt
GET  /api/health
POST /api/generate
POST /api/generate/zip
POST /api/proxy/request
```
