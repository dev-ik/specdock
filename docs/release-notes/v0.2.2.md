# SpecDock v0.2.2

SpecDock v0.2.2 fixes the generated SDK smoke check in CI environments that
have PHP Composer installed.

## Fixed

- Increased the SDK smoke test timeout to allow language toolchain checks to complete.
- Set `COMPOSER_ROOT_VERSION` for generated PHP SDK validation.
